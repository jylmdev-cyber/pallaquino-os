# Reference architecture: Web Saas

Use a browser/API boundary, server-side authorization, explicit cache ownership, transactional data, background jobs and end-to-end observability. Keep frontend independent from direct database access.

This is a decision aid, not a mandatory blueprint. Select it only after repository,
domain, risk and capability analysis; record material deviations as ADRs.
