# CLI launchers

Install the package with `pip install .` to obtain `pallaquino`, or run the
portable module directly with `python -m pallaquino_cli`. Windows users may use
the root `pallaquino.cmd`; POSIX users may invoke `cli/pallaquino`.

