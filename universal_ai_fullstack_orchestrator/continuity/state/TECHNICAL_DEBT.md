# Technical debt

- No intentional workaround is required for the portable framework.
- The repository analyzer deliberately provides conservative API/database detection; ecosystem-specific analyzers can extend it without changing the map schema.
- Numeric performance budgets remain product-specific by design and must be filled during product planning.
