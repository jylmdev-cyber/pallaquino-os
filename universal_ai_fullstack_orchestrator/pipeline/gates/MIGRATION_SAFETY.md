# MIGRATION_SAFETY

Assess table size, lock duration, backward compatibility, backfill, backup, zero downtime and rollback. Destructive changes require approval.

Record command/reviewer, timestamp, exit code or decision, result, summary and artifact in `evidence/`. Failure returns to `IMPLEMENTATION`.
