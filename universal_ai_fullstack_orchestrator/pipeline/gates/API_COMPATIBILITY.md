# API_COMPATIBILITY

Diff contracts for removed endpoints or fields, changed types, new required fields and status-code semantic changes.

Record command/reviewer, timestamp, exit code or decision, result, summary and artifact in `evidence/`. Failure returns to `IMPLEMENTATION`.
